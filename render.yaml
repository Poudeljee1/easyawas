services:
  - type: web
    name: easyawas
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn rentify.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: PYTHON_VERSION
        value: "3.13.1"
